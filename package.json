{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "",
  "private": false,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "version": "lerna version --conventional-commits --yes",
    "publish": "npm run build && npm run version && lerna publish --yes",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
